import React from 'react';

const About = () => {
  return (
    <section className='section about-section'>
      <h1 className='section-title'>About</h1>
      <p>
        This is a React web app built to utilise React hooks, styled with CSS.
      </p>
      <p>Search for your favourite cocktail and get the recipe!</p>
    </section>
  );
};

export default About;
